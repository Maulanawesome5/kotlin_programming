# Perintah docker untuk mengelola image

# menampilkan daftar image
docker image ls

# mendownload image
# docker image pull namaimage:tag
docker image pull mariadb/server:latest

# melakukan remove image
# docker image rm namaimage:tag
docker image rm mariadb/server:latest
